export const TOKEN_NOT_FOUND =
  'Your confirm token is wrong and does not exist.';
export const USER_NOT_FOUND_TOKEN = 'User who token belows do not exists.';

export const FIELD_CANNOT_BE_EMPTY = (field: string) =>
  `The ${field} cannot be empty.`;
