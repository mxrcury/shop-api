definitions:
  users:
    type: array
    items:
      - type: object
        properties:
          _id:
            type: string
          firstName:
            type: string
          lastName:
            type: string
          email:
            type: string
          photo:
            type: string
          createdAt:
            type: string
            format: date-time
          updatedAt:
            type: string
            format: date-time
  user:
    type: object
    properties:
      _id:
        type: string
      firstName:
        type: string
      lastName:
        type: string
      email:
        type: string
      photo:
        type: string
      createdAt:
        type: string
        format: date-time
      updatedAt:
        type: string
        format: date-time
